import React from 'react';
import CategoryMenu from '../components/CategoryMenu';

const Home = () => {
    return (
        <div>
            <CategoryMenu />
        </div>
    );
};

export default Home;